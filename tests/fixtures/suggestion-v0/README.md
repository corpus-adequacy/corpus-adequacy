# suggestion-v0 fixtures (#200)

Hand-written proposals for the admission harness. None came from a model; the `model-good.json`
authorship block is a shape fixture with placeholder digests. `good.json` is the obvious
counterexample for the frozen independent survivor (value 12), which is already public in the
selection's mutation bundle; it exercises the harness, not suggestion value.
